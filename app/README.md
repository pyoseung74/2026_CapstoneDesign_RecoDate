# App Structure Draft

This directory is reserved for the React Native client.

The app is intentionally split by phase, matching the confirmed product flow:

- `phase1-initial-input`
- `phase2-selection-hub`
- `phase3-customizing`
- `phase4-execution`

Shared UI state and domain types live under `src/shared`.
